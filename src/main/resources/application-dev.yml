spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: lms
    password: lms
  jpa:
    show-sql: true

logging:
  level:
    com.hosiyar.lms: DEBUG
    org.hibernate.SQL: DEBUG
